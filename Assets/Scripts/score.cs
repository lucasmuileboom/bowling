using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class score : MonoBehaviour
{/*
    throwing throwing;
    private int Score = 0;
    private int previousScore1 = 0;
    private bool Strike = false;
    private bool Spare = false;
    Dictionary<int, int> dictionary = new Dictionary<int, int>();
    void Update ()
    {
        if (throwing == null)
        {
            throwing = GameObject.Find("Ball").GetComponent<throwing>();
        }
        //print(Score);
	}
    public void getScore() 
    {
        Score++;
    }
    public void scoreIets()
    {
        if (throwing.Throwcounter == 1 && previousScore1 + Score == 10)//strike
        {            
            Strike = true;
            Spare = false;
            throwing.Throwcounter = 2;
            ScoreIets2();
        }
        else if (throwing.Throwcounter == 2 && previousScore1 + Score == 10)//spare
        {            
            Strike = false;
            Spare = true;
            ScoreIets2();
        }
        else if(throwing.Throwcounter == 1 && previousScore1 + Score != 10)
        {
            previousScore1 = Score;
            Score = 0;
        }
        else if (throwing.Throwcounter == 2 && previousScore1 + Score != 10)
        {
            Strike = false;
            Spare = false;
            ScoreIets2();
        }
    }
    private void ScoreIets2()
    {
        //dictionary.Add(Score, previousScore);
        Score = 0;
        previousScore1 = 0;
    }*/
}